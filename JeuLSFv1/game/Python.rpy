#Python:
init python:
    class Room:
        def __init__(self, name, image, x, y, suite):
            self.name = name
            self.image = image
            self.x = x
            self.y = y
            self.suite = suite
            
